<p>
  <strong><%= t("activerecord.models.user") %>:</strong>
  <%= link_to User.find_by(id: @book.user_id).name, user_path(@book.user_id) %>
</p>

<p>
  <strong><%= Book.human_attribute_name(:title) %>:</strong>
  <%= @book.title %>
</p>

<p>
  <strong><%= Book.human_attribute_name(:memo) %>:</strong>
  <%= @book.memo %>
</p>

<p>
  <strong><%= Book.human_attribute_name(:author) %>:</strong>
  <%= @book.author %>
</p>

<p>
  <strong><%= Book.human_attribute_name(:picture) %>:</strong>
  <%= image_tag(@book.picture_url) if @book.picture.present? %>
</p>

<%= link_to t("comments.index.title"), book_comments_path(@book) %> |
<% if current_user == @book.user %>
  <%= link_to t("directory.button.edit"), edit_book_path(@book) %> |
  <%= link_to t("directory.button.destroy"), @book, method: :delete, data: { confirm: t("directory.message.destroy_alert") } %>
<% end %>
